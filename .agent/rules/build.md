---
trigger: model_decision
description: Navegação do projeto — a fonte canônica é AGENTS.md e docs/ARCHITECTURE.md
---

# Navegação do projeto (consolidado)

A fonte canônica de estrutura e arquitetura é **`AGENTS.md`** (raiz) e
**`docs/ARCHITECTURE.md`**. Consulte-os ao navegar e alterar o projeto.

⚠️ A versão antiga deste guia citava `apps/mobile`, `packages/ui` e
`/components/features` — que **não existem** no código atual. Ignore qualquer
referência a eles.
